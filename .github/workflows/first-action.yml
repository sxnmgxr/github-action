name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubunuru-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodby
        run: echo "Done exit."
